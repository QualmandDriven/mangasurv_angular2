import { StringImageReplacePipe } from './string-image-replace.pipe';

describe('StringImageReplacePipe', () => {
  it('create an instance', () => {
    const pipe = new StringImageReplacePipe();
    expect(pipe).toBeTruthy();
  });
});
