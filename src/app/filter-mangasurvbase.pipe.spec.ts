import { FilterMangaSurvBasePipe } from './filter-mangasurvbase.pipe';

describe('FilterMangaSurvBasePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMangaSurvBasePipe();
    expect(pipe).toBeTruthy();
  });
});
